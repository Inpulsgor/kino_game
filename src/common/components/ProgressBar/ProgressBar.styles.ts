import { styled } from '@mui/system';
import { Box } from '@mui/material';

export const WrapperBox = styled(Box)(() => ({
  display: 'flex',
  alignItems: 'center',
  marginBottom: 4,
  padding: '12px 0',
}));

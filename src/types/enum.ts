export enum LOADING_STATUS {
  IDLE = 'IDLE',
  LOADING = 'LOADING',
}

export enum ROUTES {
  HOME = '/',
  NOT_FOUND = '*',
}

export enum NETWORK {
  main = 'mainnet-beta',
  devnet = 'devnet',
  testnet = 'testnet',
}
